title:               Graphing Relationships - Serendipity in Research, Writing, and Note-Taking
include:             ['_pages']
exclude:             ['_includes/notes_graph.json', '_notes/Templates/Meta.md']
# You may need to change the base URL depending on your deploy configuration.
# Specifically, when using GitHub Pages, the baseurl should point to where GitHub
# Pages deploys your repository (which is usually the repository name).
baseurl:             ''

# Kairos Metadata Fields
author:               Bret Zawilski
lastName:             zawilski
keywords:             serendipity, multimodality, transfer, notetaking, digital rhetoric
shortTitle:           Graphing Relationships
abstract:             Zettelkasten and other crazy stuff
publicationDate:      "2023-05-15"
googleDate:           "2023/05/15"
issue:                "28.2"
section:              topoi

# If you are using a host that cannot resolve URLs that do
# not end with .html (such as Neocities), set this to 'true'.
use_html_extension:  true

# Set to `true` to open non-internal links in new tabs, or
# set to `false` to open non-internal links in current tab.
open_external_links_in_new_tab: true

# Set to `true` to replace tweet URLs with Twitter embeds.
# Note that doing so will negatively the reader's privacy
# as their browser will communicate with Twitter's servers.
embed_tweets: false

permalink:           pretty
relative_permalinks: false

plugins:
  - jekyll-last-modified-at

sass:
  sass_dir:          _sass
  style:            :compressed

collections:
  notes:
    output: true
    permalink: /:slug

defaults:
  - scope:
      path: "**/*"
    values:
      layout: "default"
  - scope:
      path: "_notes/**/*.md"
    values:
      layout: "note"